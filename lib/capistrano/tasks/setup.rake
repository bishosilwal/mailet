namespace "deploy" do
  desc "setting up server"
  # task :bundle_update do
  #   on roles(:all) do |host|
  #     puts "update bundler"
  #     execute "apt install rubygems"
  #     execute "gem install bundle"
  #     execute 'bundle update --bundler'
  #   end
  # end
  #
  # before 'bundler:config', :bundle_update
end
